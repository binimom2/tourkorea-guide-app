# 투어코리아 앱 모음 — 작업 규칙

사장님(비개발자)과 한국어로 일한다. 보고·화면 문구는 **최대한 간결하게**, 그 자리에 필요한 것만.

## 무엇이 어디 있나
배포: Cloudflare Pages (`tourkorea-guide-app.pages.dev`) — `main`에 푸시하면 1~2분 뒤 자동 반영. 자료: Supabase `guide_data` 표(키 하나 = 자료 한 덩이).

| 경로 | 무엇 |
|---|---|
| `index.html` | 가이드 정산서(가이드앱) |
| `travel/` | Travel 수배창(예약·원가서) |
| `golf/` | 직원·여행사 실시간 골프 견적기 |
| `golf/admin/` | 요금·코드 관리(차량·핸들링·가이드) |
| `golf/site/` | 손님 골프 사이트(위브투어/투어코리아) |
| `golf/site/admin/` | 사이트 관리(골프장·호텔·스파 내용 + 요금 입력, 예약요청, 견적 기록, 여행사 회원) |
| `golf/site/voucher/` | 바우처·인보이스(토큰 링크) |
| `forms/` | 수배 공용 인보이스·바우처 빈 양식 |
| `functions/api/` | 서버 함수(golf-price 요금계산, golf-book 예약·바우처·인보이스, agency 여행사, fx/superrich 환율) |

## 꼭 지킬 것
- **에러·데이터 사고가 최우선.** 사고를 알리면 하던 일을 멈추고 바로 대응. 자료를 건드리는 코드는 실패 경로까지 시험.
- **실데이터 파괴 시험 금지.** 저장 로직은 덮어쓰기 금지 — 서버 최신본을 다시 읽어 병합하고, 지우기 전 백업(`golf_bak_*` 등).
- **한글 파일에 `sed -i`·`perl -pi` 금지** — 인코딩이 통째로 깨진다. 편집 도구로만 고친다.
- **Travel(`travel/`)은 낮에 직원이 입력 중이라 커밋만 하고 푸시는 18:00 이후.** 그 밖(골프·가이드앱 등)은 고치면 바로 푸시·배포해도 된다(사장님 허락).
- 숫자 입력칸에 `0`을 미리 넣지 않는다(입력이 밀린다).
- 손님 문구에 「골라/고르」 쓰지 않고 「선택」.
- 입력창은 최소로. 이미 다른 곳에 있는 기능·안 쓰는 항목은 화면에서 뺀다(자료는 유지).
- **푸시 전 고친 페이지의 `<script>` 문법 검사 필수**(따옴표 하나 남아 사이트 관리 전체가 멈춘 적 있음). 함수 몇 개만 시험하면 못 잡는다.
- 배포 뒤에는 가능하면 브라우저로 실제 화면을 확인하고 결과를 그대로 보고.
- 커밋 메시지는 한국어로 무엇을 왜 바꿨는지.

## 골프 요금 규칙(자주 헷갈림)
- 이름의 정본은 **사이트 관리 이름칸 하나**. 요금표(`golf_pricing`)는 이름(띄어쓰기·대소문자 무시)으로 이어진다. 관리 화면 저장 때 요금표 이름을 맞추고(`syncPriceNames`), 목록에 없는 옛 요금·엑셀 시드는 지운다(`pruneOldPricing`). **노출 체크만 끈 항목은 요금까지 유지.**
- 호텔 요금은 **객실 타입별 요금(rooms[].rates)만** 인정. 옛 1박 단가(perNight)·골프 옛 시즌줄(seasons)은 안 읽는다.
- 요금 없는 곳은 「요금 문의」로 담긴다(정상).
- 여행사 인보이스: 컨펌 → 초안 → 어드민 확인·수정 → 발행(디파짓 인보이스) → 디파짓 입금 확인 → 잔금 인보이스·확정서.
- 예약요청 🗑 삭제는 테스트 기간용 — 사장님이 나중에 막기로 함.
